class CalculadoraPagamento:
    def calcular_salario(self, contrato, horas):
        return contrato.salario * horas / 160

    def calcular_liquido(self, bruto, descontos, beneficios):
        return bruto - descontos + beneficios